# Welcome to your TypeScript API  👋


## Get started
Code Setup Commands:

```bash
npm install typescript tsx @types/node -D
npx tsc --init
npm lint
npm add express @types/express
```

Source of entry 

```bash
index.ts
```

1. Install dependencies

   ```bash
   npm install
   npm install cors
   ```

2. Start the app

   ```bash
   npm start
   ```

3. Once the app is ready you will get a confirmation with localhost link
Server is running at http://localhost:4000


